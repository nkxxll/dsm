# Agents.md for jai-arden

- the jai compiler bin is at `jai-linux`
- this project uses jujitsu `jj` instead of `git`
- this project is written in `jai` if there are question about how some functions work or what
functions there are in the std lib look at `~/git/jai/modules`
